// Styles
import styles from "../../../styles/layout/nav.module.scss";

const Nav = () => {
  return (
    <nav className={styles.nav}>
      Fourier Series Visualizer
    </nav>
  );
};

export default Nav;
